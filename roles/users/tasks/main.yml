---
- name: create deploy group
  group: name={{ deploy_user }} state=present

- name: create deploy user
  user: name={{ deploy_user }} state=present group={{ deploy_group }}

- name: add key for deploy user
  authorized_key: user={{ deploy_user }} key=https://github.com/dunn.keys
