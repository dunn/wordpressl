---
- name: Add a TLS certificate
  remote_user: deploy
  hosts: all

  vars_files:
    - session.yml

  vars:
    project_dir: /var/www/html

    # After setup, root SSH access will be removed
    deploy_user: deploy
    deploy_group: apache
    become_user: deploy

  roles:
    - { role: tls }
